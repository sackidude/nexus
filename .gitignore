# Contains passwords and login information
.env

# Ignore stupid vscode things
.vscode

# Useful for backing up a file for debugging
*.bak

# This is for the scripts/node
node_modules
package-lock.json

# All the pictures shouldn't be on the git repo, instead just the zipped versions
static/images/trial-*/*.jpg

# Rust 
/target
